import { defineMessages } from '@bloom/intl'

export default defineMessages({
  invalidEmailOrPassword: {
    id: 'api.auth.invalid_email_or_password',
    defaultMessage: 'Login or password incorrect',
  },
})
